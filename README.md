# 🤗 Бот поддержки с добрыми советами

Проект телеграм-бота, который отправляет пользователям добрые советы и слова поддержки. 
Бот интегрирован с Django веб-приложением для управления базой советов.

## 🌟 Возможности

- **🎲 Случайный совет** - отправляет случайный совет из базы данных
- **📁 По категориям** - фильтрация советов по категориям
- **💪 Мотивация** - советы для поднятия духа
- **🤗 Утешение** - поддержка в трудные моменты
- **✨ Вдохновение** - идеи для творчества
- **🧠 Мудрость** - жизненные уроки
- **⚙️ Админ-панель** - управление базой советов через веб-интерфейс

## 🏗️ Архитектура проекта

```
django_support_bot/
├── advice_project/          # Django-проект
│   ├── advice_app/         # Приложение с советами
│   │   ├── models.py       # Модель Advice
│   │   ├── views.py        # API endpoint
│   │   ├── admin.py        # Админка
│   │   └── urls.py         # Маршруты API
│   └── advice_project/     # Настройки Django
├── telegram_bot/           # Телеграм-бот
│   └── bot.py             # Основной код бота
├── manage.py              # Django CLI
├── requirements.txt       # Зависимости
├── .env                  # Переменные окружения
└── README.md             # Этот файл
```

## 🚀 Быстрый старт

### 1. Клонирование и настройка

```bash
# Клонируйте репозиторий
git clone <ваш-репозиторий>
cd django_support_bot

# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
SECRET_KEY=ваш-секретный-ключ-для-django
DEBUG=True
TELEGRAM_BOT_TOKEN=ваш-токен-от-botfather
```

**Как получить токен бота:**
1. Откройте Telegram
2. Найдите `@BotFather`
3. Отправьте `/newbot`
4. Следуйте инструкциям и скопируйте токен

### 3. Настройка базы данных

```bash
# Примените миграции
python manage.py migrate

# Создайте суперпользователя для админки
python manage.py createsuperuser
# Введите имя пользователя, email и пароль
```

### 4. Добавление начальных данных

Откройте админ-панель и добавьте несколько советов:

1. Перейдите по адресу: `http://127.0.0.1:8000/admin/`
2. Войдите под созданным аккаунтом
3. В разделе "Advice" нажмите "Add advice"
4. Добавьте несколько советов в разных категориях

**Примеры категорий:**
- `motivation` - 💪 Мотивация
- `comfort` - 🤗 Утешение  
- `inspiration` - ✨ Вдохновение
- `wisdom` - 🧠 Мудрость

### 5. Запуск проекта

**Терминал 1 - Django сервер:**
```bash
python manage.py runserver
```

**Терминал 2 - Телеграм бот:**
```bash
python telegram_bot/bot.py
```

### 6. Тестирование

1. **Проверьте API:** Откройте в браузере `http://127.0.0.1:8000/api/advice/`
2. **Проверьте бота:** Найдите своего бота в Telegram и отправьте `/start`
3. **Протестируйте все функции:**
   - Нажмите "🎲 Случайный совет"
   - Нажмите "📁 По категории" и выберите категорию
   - Нажмите "ℹ️ О боте"

## 📡 API Endpoints

### Получить случайный совет
```
GET /api/advice/
```

**Ответ:**
```json
{
    "text": "Текст совета...",
    "category": "💪 Мотивация",
    "id": 1
}
```

### Получить совет по категории
```
GET /api/advice/?category=<category_name>
```

**Доступные категории:**
- `motivation`
- `comfort` 
- `inspiration`
- `wisdom`

**Пример:**
```
GET /api/advice/?category=motivation
```

## 🛠 Технологии

- **Python 3.8+** - основной язык программирования
- **Django 4.2** - веб-фреймворк
- **python-telegram-bot 20.6** - библиотека для Telegram API
- **SQLite** - база данных (по умолчанию)
- **Requests** - HTTP клиент для API запросов

## 📁 Структура кода

### Модель Advice (`advice_app/models.py`)

```python
class Advice(models.Model):
    CATEGORY_CHOICES = [
        ('motivation', '💪 Мотивация'),
        ('comfort', '🤗 Утешение'),
        ('inspiration', '✨ Вдохновение'),
        ('wisdom', '🧠 Мудрость'),
    ]
    
    text = models.TextField(verbose_name='Текст совета')
    category = models.CharField(max_length=20, choices=CATEGORY_CHOICES)
    created_at = models.DateTimeField(auto_now_add=True)
    is_active = models.BooleanField(default=True)
```

### API View (`advice_app/views.py`)

```python
@csrf_exempt
@require_GET
def get_random_advice(request):
    # Получает случайный активный совет
    # Поддерживает фильтрацию по категории
```

### Телеграм бот (`telegram_bot/bot.py`)

Основные функции:
- `start()` - приветственное сообщение
- `get_random_advice()` - отправка случайного совета
- `choose_category()` - выбор категории
- `get_advice_by_category()` - совет по категории
- `about()` - информация о боте

## 🔧 Разработка и расширение

### Добавление новых категорий

1. В `models.py` добавьте категорию в `CATEGORY_CHOICES`:
```python
CATEGORY_CHOICES = [
    ...
    ('new_category', '🌈 Новая категория'),
]
```

2. В `bot.py` добавьте кнопку в клавиатуру:
```python
category_keyboard = ReplyKeyboardMarkup([
    ...
    ['🌈 Новая категория'],
])
```

### Добавление новых команд бота

```python
# 1. Создайте функцию-обработчик
async def new_command(update, context):
    await update.message.reply_text("Ответ на новую команду")

# 2. Зарегистрируйте обработчик
application.add_handler(CommandHandler("new", new_command))
```

### Изменение дизайна клавиатуры

Отредактируйте переменные `main_keyboard` и `category_keyboard` в `bot.py`:

```python
main_keyboard = ReplyKeyboardMarkup(
    [['🎲 Случайный совет', '📁 По категории'], ['ℹ️ О боте', '🌟 Новый раздел']],
    resize_keyboard=True
)
```

## 🚀 Деплой

### Heroku

1. Создайте файл `Procfile`:
```
web: python manage.py runserver 0.0.0.0:$PORT
worker: python telegram_bot/bot.py
```

2. Создайте файл `runtime.txt`:
```
python-3.9.0
```

3. Настройте базу данных PostgreSQL:
```bash
heroku addons:create heroku-postgresql:hobby-dev
```

### PythonAnywhere

1. Загрузите файлы через веб-интерфейс
2. Создайте виртуальное окружение
3. Установите зависимости
4. Настройте веб-приложение (WSGI)
5. Настройте задачу по расписанию для бота

## 🐛 Устранение неисправностей

### Бот не запускается

**Ошибка:** `AttributeError: type object 'Update' has no attribute 'ALL_UPDATES'`

**Решение:** Удалите параметр `allowed_updates` из `run_polling()` или обновите версию библиотеки:
```bash
pip install --upgrade python-telegram-bot
```

### API возвращает 404

**Проверьте:**
1. Запущен ли Django сервер (`python manage.py runserver`)
2. Правильно ли настроены URL в `urls.py`
3. Есть ли активные советы в базе данных

### Бот не отвечает на команды

**Проверьте:**
1. Правильный ли токен в `.env` файле
2. Запущен ли бот (`python telegram_bot/bot.py`)
3. Не заблокирован ли бот в Telegram

## 📈 Планы развития

- [ ] Добавление системы лайков для советов
- [ ] Ежедневная рассылка советов
- [ ] Веб-интерфейс для пользователей
- [ ] Мультиязычная поддержка
- [ ] Интеграция с AI для генерации советов
- [ ] Статистика популярности советов
- [ ] Экспорт/импорт базы советов

## 🤝 Участие в разработке

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📄 Лицензия

MIT License - смотрите файл LICENSE

## 📞 Контакты

Если у вас есть вопросы или предложения:
- Создайте Issue в репозитории
- Напишите в Telegram: [@your_username]

---